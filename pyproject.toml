[project]
name = "boot-asteroids"
version = "0.1.0"
description = "Asteroids with pygame from boot.dev course."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
